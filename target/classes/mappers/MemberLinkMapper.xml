<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.megalink.study.mappers.MemberLinkMapper">
    <insert id="insertMemberLink">
        INSERT into memberlink (
        ROLEACLASS,
        ROLEBCLASS,
        ROLEAID,
        ROLEBID,
        PRINCIPALID
        ) values(
        #{ROLEACLASS},
        #{ROLEBCLASS},
        #{ROLEAID},
        #{ROLEBID},
        #{PRINCIPALID}
        );
    </insert>
</mapper>